import styled from "styled-components";

const AdvertisingCss = styled.section`
    width: 100%;
    margin-top: 3em;
    margin-bottom: 1em;
    padding: 1.2em 4em;
    text-align: center;
    background-color: var(--blue);
    color: #fff;
    line-height: 1.5em;
    border-radius: 1em;
    /* review this color  */
    box-shadow: 1px 8px 10px var(--dark-blue) ;

    strong{
        font-weight: bold;
    }
`;

export default AdvertisingCss;